# راهنمای استفاده از API عمومی (Public API)

این مستندات به شما کمک می‌کند تا بتوانید سیستم‌های خود (مانند وب‌سایت، اپلیکیشن موبایل، یا سرویس‌های بک‌اند) را به طور مستقیم به موتور جستجوی اختصاصی خود متصل کنید.

## دریافت API Key
برای استفاده از API ابتدا باید یک کلید دسترسی (API Key) دریافت کنید:
۱. وارد پنل مدیریت (داشبورد) شوید.
۲. پروژه مورد نظر خود را انتخاب کنید.
۳. به تب **API Keys** بروید.
۴. روی دکمه **Create New Key** کلیک کرده و نامی برای آن انتخاب کنید (مانند "وب‌سایت اصلی").
۵. کلید تولید شده را کپی کنید. 

> **توجه:** کلیدهای API حاوی دسترسی به داده‌های پروژه شما هستند؛ لطفاً از قرار دادن آن‌ها در کدهای عمومی سمت کلاینت (مثل سورس‌کد جاوا اسکریپت در مرورگر بدون محدودیت) خودداری کنید و ترجیحاً درخواست‌ها را از طریق بک‌اند خود ارسال نمایید.

---

## احراز هویت (Authentication)
در تمامی درخواست‌هایی که به سمت API ارسال می‌کنید، باید کلید خود را در Header تحت عنوان `x-api-key` ارسال کنید.

**نمونه Header:**
```http
x-api-key: sk_YOUR_API_KEY_HERE
```

---

## اندپوینت‌ها (Endpoints)

آدرس پایه (Base URL) سرویس:
```text
http://localhost:4000/public/v1
```
*(توجه: در محیط پروداکشن این آدرس به دامنه اصلی تغییر خواهد کرد)*

### ۱. بررسی وضعیت دسترسی (Info)
از این مسیر می‌توانید برای تست صحت API Key خود استفاده کنید.

**مسیر:** `GET /info`

**نمونه درخواست (cURL):**
```bash
curl -X GET http://localhost:4000/public/v1/info \
  -H "x-api-key: sk_YOUR_API_KEY_HERE"
```

**نمونه پاسخ موفق (JSON):**
```json
{
  "projectId": "123e4567-e89b-12d3-a456-426614174000",
  "message": "Authentication successful. You have access to this project.",
  "apiKeyName": "وب‌سایت اصلی"
}
```

---

### ۲. جستجو در لینک‌ها و مقالات (Document Search)
اگر از خزنده‌ی وب (Crawler) ما برای ایندکس کردن محتوای سایت و مقالات خود استفاده می‌کنید، از این مسیر برای جستجو استفاده کنید.

**مسیر:** `GET /search`

**پارامترهای Query (کوئری):**
- `q` (اجباری): کلمه کلیدی برای جستجو.

**نمونه درخواست:**
```bash
curl -X GET "http://localhost:4000/public/v1/search?q=یادگیری+ماشین" \
  -H "x-api-key: sk_YOUR_API_KEY_HERE"
```

**نمونه پاسخ:**
```json
[
  {
    "id": "doc_123",
    "title": "یادگیری ماشین چیست؟",
    "url": "https://example.com/machine-learning",
    "content": "متن خلاصه مقاله در اینجا قرار دارد..."
  }
]
```

---

### ۳. جستجو در محصولات فروشگاهی (Product Search)
اگر سیستم شما فروشگاهی است و محصولات شما ایندکس شده‌اند، این مسیر امکان فیلتر کردن پیشرفته‌ی محصولات را به شما می‌دهد.

**مسیر:** `GET /products/search`

**پارامترهای Query (کوئری):**
- `q` (اجباری): نام محصول، دسته‌بندی یا برند.
- `min_price` (اختیاری): حداقل قیمت.
- `max_price` (اختیاری): حداکثر قیمت.
- `brand` (اختیاری): فیلتر دقیق بر اساس نام برند.
- `in_stock` (اختیاری): دریافت محصولات موجود (`true` یا `false`).

**نمونه درخواست:**
```bash
curl -X GET "http://localhost:4000/public/v1/products/search?q=گوشی&min_price=10000000&in_stock=true&brand=Samsung" \
  -H "x-api-key: sk_YOUR_API_KEY_HERE"
```

**نمونه پاسخ:**
```json
{
  "results": [
    {
      "id": "prod_789",
      "title": "گوشی موبایل سامسونگ",
      "price": 15000000,
      "currency": "Toman",
      "brand": "Samsung",
      "in_stock": true,
      "url": "https://example.com/product/samsung-mobile",
      "image_url": "https://example.com/images/samsung.jpg"
    }
  ],
  "facets": [
    {
      "field_name": "brand",
      "counts": [
        { "value": "Samsung", "count": 1 }
      ]
    }
  ]
}
```

---

## کدهای خطا (Error Codes)
در صورت بروز خطا در استفاده از سیستم، کدهای HTTP زیر برگردانده می‌شوند:
- **401 Unauthorized**: در صورتی که API Key ارسال نشده باشد، معتبر نباشد و یا لغو (Revoke) شده باشد.
- **404 Not Found**: در صورتی که پروژه مورد نظر یافت نشود و یا دسترسی به آن امکان‌پذیر نباشد.
- **500 Internal Server Error**: خطای داخلی سرور در هنگام جستجو.
